# 운영 환경변수 템플릿
# 서버에서 .env.prod 파일로 복사 후 실제 값 입력
# docker compose --env-file .env.prod -f docker-compose.prod.yml up -d

# Database
DB_USER=notary
DB_PASSWORD=change-me-strong-password
DB_NAME=notary

# Application
SECRET_KEY=change-me-random-64-char-string
